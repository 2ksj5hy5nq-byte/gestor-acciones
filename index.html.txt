<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<title>Gestor de Acciones</title>

<style>
body{font-family:Arial;background:#0f172a;color:white;padding:15px}
input,select,button{padding:6px;margin:4px}
table{width:100%;margin-top:10px;border-collapse:collapse}
td,th{border-bottom:1px solid #334155;padding:4px;text-align:center}
</style>
</head>

<body>
<h3>📈 Gestor de Acciones</h3>

<input id="accion" placeholder="Acción">
<select id="tipo">
<option>Compra</option>
<option>Venta</option>
</select>
<input id="cantidad" type="number" placeholder="Cantidad">
<input id="precio" type="number" step="0.01" placeholder="Precio">
<input id="fecha" type="date">
<button onclick="guardar()">Guardar</button>

<table>
<thead>
<tr><th>Acción</th><th>Tipo</th><th>Nº</th><th>Precio</th><th>Fecha</th><th>Total</th></tr>
</thead>
<tbody id="tabla"></tbody>
</table>

<script>
let ops=JSON.parse(localStorage.getItem("ops"))||[];
function guardar(){
ops.push({
a:accion.value,
t:tipo.value,
n:+cantidad.value,
p:+precio.value,
f:fecha.value
});
localStorage.setItem("ops",JSON.stringify(ops));
pintar();
}
function pintar(){
tabla.innerHTML="";
ops.forEach(o=>{
tabla.innerHTML+=`<tr>
<td>${o.a}</td><td>${o.t}</td><td>${o.n}</td>
<td>${o.p}€</td><td>${o.f}</td>
<td>${(o.n*o.p).toFixed(2)}€</td></tr>`;
});
}
pintar();
</script>
</body>
</html>